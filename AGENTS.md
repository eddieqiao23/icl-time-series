When generating graphs, keep labels simple but descriptive. Avoid bold or
unnecessary typography unless it is genuinely useful. Align axes across
comparable panels. Use default plotting colors unless otherwise specified.
When plotting layer numbers, run IDs, task indices, or other discrete integer
indices on an axis, use integer tick marks only.
